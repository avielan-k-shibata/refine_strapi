export interface IItem {
    id: number;
    name: string;
    status: boolean;
    createdAt: string;
}

export interface IItemFile {
    id: number;
    name: string;
    status: boolean;
    createdAt: string;
}

export interface ICategory {
    id: number;
    name: string;
}